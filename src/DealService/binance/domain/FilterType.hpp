#ifndef BINANCE_FILTER_TYPE_H
#define BINANCE_FILTER_TYPE_H

namespace binance {
    enum class FilterType
    {
        PRICE_FILTER,
        LOT_SIZE,
        MIN_NOTIONAL,
        NOTIONAL
    };
}

#endif
